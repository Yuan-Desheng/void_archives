# Interactive Tech Tutorial Platform — 开发计划

> 版本 v0.1 · 2026-07-17 · Owner：崔战伟
> 流程依据：[[项目立项与研发流程规范]]（六阶段 + 四评审，单人全栈负责制）
> 项目定级：**小型 / 简化流程**（§3.4）—— 无合同、不对外交付 → 免立项评审，评审收敛为交付评审④
> 但知识库按 §8.2 全套建，因为本项目同时是**规范落地的第一个试验田**，要跑通 `工作流-原型→设计→开发→测试→部署` 全链路

---

## 一、这个项目到底在做什么（一句话）

**做一个引擎，不是做一个 React 教程。** React 只是塞进去的第一包内容（62 篇现成笔记）。
判断成败的标准只有一条：**加一个 `vue.json` 就能跑出 Vue 教程，不改任何引擎代码**（对应 R8）。
如果最后做出来的是「一个好看的 React 教程网站，但换主题要改代码」——这个项目就失败了。

## 二、三个已定决策（2026-07-17，详见 `99-会议与决策/`）

| 决策 | 结论 | 对计划的影响 |
|---|---|---|
| **D1 沙箱** | Sandpack 真实运行 | 引入 `@codesandbox/sandpack-react`；移动端要做体积/布局降级 |
| **D2 引导对象** | **仅平台自身 UI（host）** | **砍掉 iframe 探针 + postMessage 通信**，引擎工作量减半；Schema 保持原样不加 scope |
| **D3 后端** | Clerk + Postgres + Drizzle 全套进 MVP | 不是纯静态站；工期 +1 周；Vercel 需配 Neon/Supabase + Clerk 环境变量 |

> **D2 的产品含义，请复核**：教程引导的是「怎么用这个平台」（点 Run、切文件、看预览区），
> 「怎么写 React」靠 MDX 讲解 + Sandpack 自己动手。聚光灯**不会**追学习者写出来的按钮。
> 这让产品从「Vue3 官方教程那种手把手」偏向「MDX 教程 + 可玩沙箱 + 平台导览」。
> 若这不是你要的，现在改成本最低（改 Schema 加 scope）；进开发后改 = 架构级变更。

## 三、最大风险：内容，不是代码（必须先看这条）

> [!danger] 全项目最大成本项是「62 篇笔记 → tutorial steps」，不是写引擎
> 引擎大约 2 周能出。**内容转换可能吃掉 3–4 周**，而且是纯体力活、AI 不一定省力。
> 盘点结果：canvas 62 节点、**0 条 edge**（全孤立）→ 章节顺序无法推导，得人工定。
> Router 24 篇 + Hooks 17 篇占了 66%，这两块是重灾区。

**Q3（未决）**：62 篇怎么转？三条路，成本差一个量级：

| 路径 | 做法 | 成本 | 产出质量 |
|---|---|---|---|
| **A · 直接 MDX 渲染** | 笔记原文当 MDX 直接渲染，不拆 steps，只在章节级做导航 | ~3 天 | 就是个笔记网站，R3/R4/R5 全废 |
| **B · AI 批量转 + 人工校**（建议） | AI 把每篇拆成 steps JSON，人工校验 | ~1.5 周 | 中等，需抽检臆造 |
| **C · 人工逐篇改写** | 一篇篇手写 steps + 配套 defaultCode | 3–4 周 | 最好 |

**建议**：MVP 只做 **8–10 篇样板**（入门3 + Hooks 核心5），用路径 C 打磨到最好，验证引擎；剩下 52 篇留到 v2 用路径 B 批量补。
**理由**：先证明「引擎 + 一包好内容」成立，再规模化。62 篇全量搬运是典型的「先把体力活干完再发现方向错了」。

**Q4（未决）**：章节顺序谁定 → 建议 Owner 直接拍一版线性顺序（入门 → 组件 → Hooks → CSS → Router → Zustand → 工具 → 原理），不必完美。

## 四、六阶段计划

### 阶段① 需求 ✅ 已完成（2026-07-17）
- 产出：`01-需求/原始需求.md`（R1–R9 逐条编号、范围边界含「不做什么」、验收口径初稿）
- 决策：D1/D2/D3 落 `99-会议与决策/`
- 简化流程免评审①，但 DoD 已自检：功能清单编号 ✅ / 不做什么 ✅ / 验收口径 ✅ / 待确认项已标注（Q3/Q4）✅

### 阶段② 原型 ✅ 已产出，待 Owner 冻结（2026-07-17）
- 工具：`工作流-原型`（req-to-proto，26 agent / ~31min）→ `02-原型/`
- 产出：18 个 HTML + 6 份 spec + manifest，5 模块（教程目录 / 课程讲解 / 引导引擎 / 代码沙箱 / 学习进度）
- 视觉：深色仪表盘风（Primary #4C6FFF / Accent #22D3EE / Spotlight #FDBA33）
- **验收结论**（Playwright 实测，详见 `02-原型/验收记录-2026-07-17.md`）：

  | 要证明的事 | 结论 |
  |---|---|
  | 1. 引导引擎跑通（R3/R4/R5） | ✅ **真实跑通**：聚光灯定位与 `spotlightPadding` 精确吻合；click 阻塞连点无法绕过；完成动作 2s 后自动推进；resize 重算正确；降级不崩已内置 |
  | 2. 配置驱动换主题（R8） | ⚠️ **未证明**：steps 硬编码在 HTML `const` 里，非外部 JSON。属原型形态局限，**须在阶段③以架构回答** |
  | 3. 375px 不塌（R7） | ⚠️ 溢出 4px，元凶是原型自带的 `#stepJump` 调试控件，**产品 UI 中不存在** → 不算违规，阶段④重测 |

- 其他：18 页互链 **0 死链** ✅ / Console 无实质报错 ✅ / AI 主动拦下「作者后台、用户权限、评论社区」等过度设计 ✅
- ❗ `02-原型/README.md` 自述过时错误（自称「视觉原型、交互后续补充」），与实测不符，待修正
- 冻结：自用项目无外部客户 → **Owner 自己书面冻结**，签在 `02-原型/验收记录-2026-07-17.md` §七

### 阶段③ 设计
- 工具：`工作流-设计`（proto-to-design）→ `03-设计/`
- 必须回答的技术点：
  - **引擎抽象**：TutorialPackage → 运行时状态机的映射；selector 失效/元素不存在时的降级（DoD 要求「不崩」）
  - **聚光灯实现**：SVG mask vs `box-shadow: 0 0 0 9999px` vs backdrop-filter — 三选一，要有理由
  - **Sandpack 集成**：bundle 体积、SSR 边界、移动端降级策略
  - **数据库 ER**：user / progress / tutorial_package —— 进度粒度到 step 还是 chapter？
  - **接口契约**：进度读写 API
- **建议先做 spike**（规范 §五③ 允许并行）：聚光灯 + Sandpack 在移动端共存，这是最可能翻车的点，先写个 50 行 demo 验
- DoD：架构图/数据流/ER/接口契约齐全 + 选型有理由 + 安全运维非功能性有方案

### 阶段④ 开发
- 工具：`工作流-开发`（design-to-code）
- 拆成可独立上线的小步：
  1. 脚手架：Next.js 15 + TS + Tailwind v4 + shadcn/ui
  2. **引擎核心**（无 UI，纯逻辑 + 单测）：状态机、selector 解析、waitFor 阻塞
  3. 引导 UI 层：高亮框 + 聚光灯遮罩 + resize 重定位
  4. 内容层：MDX 渲染 + 章节/步骤导航 + 进度指示
  5. Sandpack 接入
  6. 后端：Drizzle schema + 迁移（幂等）+ 进度 API + Clerk
  7. 响应式打磨
- 前端复用原型（规范 §五④）
- 人工必检：引擎边界条件（selector 找不到 / 元素被卸载 / 快速连点下一步）逐段人审；公开库零明文凭据

### 阶段⑤ 测试
- 工具：`工作流-测试`（code-to-test）
- 硬门槛：关键路径用例全过 / 通过率 ≥ 90% / 0 个未归类疑似 code-bug
- 关键路径 = 引擎状态机 + selector 降级 + 进度读写

### 阶段⑥ 交付部署 →【评审④ 交付评审，必须显式通过】
- 工具：`工作流-部署`（app-to-deploy）+ **强制 `/security-review`**
- 本项目部署目标是 Vercel，非离线包 → `工作流-部署` 的净室演练意义有限，重点放在：
  - Clerk / Postgres 环境变量不入库（红线：公开仓库零明文）
  - `/security-review` 无未处理高危项

## 五、工期估算（Owner 单人 + AI）

| 阶段 | 估算 | 备注 |
|---|---|---|
| ① 需求 | 0.5 天 | ✅ 已完成 |
| ② 原型 | 3–4 天 | timebox：≤ 2 轮迭代 |
| ③ 设计（含 spike） | 3 天 | spike 单独 1 天 |
| ④ 开发 | 2.5 周 | 引擎 1 周 / 后端 0.5 周 / UI+响应式 1 周 |
| ⑤ 测试 | 3 天 | 与开发并行起 |
| ⑥ 交付 | 2 天 | |
| **内容（8–10 篇样板）** | **1 周** | **与④并行，但别低估** |
| **合计** | **约 5–6 周** | 全量 62 篇另计 +2 周（v2） |

## 六、基础设施（规范 §四，开工前就位）

- [x] 项目知识库（`00-` ~ `99-` 八目录）
- [x] 项目 AI 机器人 = Claude Code + 本知识库
- [ ] **代码仓库**：待建。建议 Gitee 私库（`omylove`），或 GitHub（若走 Vercel 集成部署更顺）→ **需 Owner 定**
- [ ] 脱敏基线：`.env` 不入库；Clerk key / Postgres 连接串只进私有密码管理器
- N/A 决策对口人 / 安全维度责任人（自用项目，无委员会）

## 七、下一步（二选一）

1. **等原型跑完** → 打开 `02-原型/index.html` 验收，重点看「引导引擎三件事」是否被证明
2. **现在就定 Q3/Q4** → 章节顺序 + 内容转换路径，这两个不定，阶段③设计会卡住

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-07-17 | v0.1 | 初版。基于 ITTP 提示词 + 规范六阶段，含 D1/D2/D3 决策 |
